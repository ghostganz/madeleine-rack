

* Expand Madeleine so that you can pass along an extra context, that's sent as argument
  to every execute(system) ... e.g. execute(system, context)
  And: Since it needs to be present at replay too, it has to be sent along when creating
  the whole madeleine system.

* Pass system seed and filename in somewhere
