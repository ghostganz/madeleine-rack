

* Expand Madeleine so that you can pass along an extra context, that's sent as argument
  to every execute(system) ... e.g. execute(system, context)
  And: Since it needs to be present at replay too, it has to be sent along when creating
  the whole madeleine system.

* Some nice integration with Rails for how/when to make snapshots?

* Start writing tests

* Handling Time?

* Minimize bytes written to log for each request

* Update to current version of Rack spec

* Investigate if Thin's async stuff can be supported

* Don't assume env supports dup(), copy the env in a more
  controlled way?

* Add .ruby-version
